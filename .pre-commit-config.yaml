# .pre-commit-config.yaml
repos:
  - repo: https://github.com/hhatto/autopep8
    rev: v2.3.2 # Use the latest stable version (check the repository's releases)
    hooks:
      - id: autopep8
        args: ["--in-place", "--aggressive", "--aggressive"] # Forcefully apply fixes
        types: [python]